# Bare DNS hostname only: do not include https:// or a path.
MCP_PUBLIC_HOSTNAME=mcp.example.com

# Optional. Leave empty to use the production DeepBiology backend.
DEEPBIOLOGY_BASE_URL=

# Do not set DEEPBIOLOGY_API_KEY here. Each remote client supplies its own
# key in the Authorization: Bearer header on every MCP request.
